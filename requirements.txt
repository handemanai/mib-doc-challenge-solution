# Complete runtime dependency closure, pinned to the versions in the verified
# scoring image. Keep this list package-for-package aligned with the pip install
# block in the Dockerfile; this file exists so a Python 3.11 local checkout can
# reproduce that environment for the test suite.
#
# The pins are load-bearing rather than incidental. RapidOCR's image
# preprocessing and the recognizer's numerics are part of the measured result,
# so resolving to different versions changes OCR output and invalidates the
# reported scores. opencv-python is listed explicitly because rapidocr
# constrains it only as >=4.5.1.48 with no upper bound.
#
# Requires Python 3.11-3.13; onnxruntime 1.20.1 publishes no wheel for 3.14.
# The image is built on python:3.11-slim.

pymupdf==1.28.0
rapidocr-onnxruntime==1.4.4
onnxruntime==1.20.1
rapidfuzz==3.14.5
numpy==2.2.6
opencv-python==4.11.0.86
Pillow==12.3.0
PyYAML==6.0.3
Shapely==2.1.2
coloredlogs==15.0.1
flatbuffers==25.12.19
humanfriendly==10.0
mpmath==1.3.0
packaging==26.2
protobuf==7.35.1
pyclipper==1.4.0
six==1.17.0
sympy==1.14.0
tqdm==4.70.0
